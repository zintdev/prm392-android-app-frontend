<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- API key cho Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Launcher: mở MainActivity với BottomNavigation trước -->
        <activity
            android:name=".presentation.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ggmap -->
        <activity
            android:name=".presentation.activity.MapsActivity"
            android:exported="true"/>

        <!-- Location Picker -->
        <activity
            android:name=".presentation.activity.LocationPickerActivity"
            android:exported="false"
            android:label="Chọn địa chỉ" />

        <!-- login&regis -->
        <activity
            android:name=".presentation.activity.LoginActivity"
            android:exported="true"
            android:label="Login" />

        <activity
            android:name=".presentation.activity.RegisterActivity"
            android:exported="true"
            android:label="Register" />

        <activity android:name=".presentation.activity.AdminMainActivity"
            android:exported="true"
            android:label="Admin" />

        <activity android:name=".presentation.activity.MainActivity"
            android:exported="true"
            android:label="User" />
            
        <activity
            android:name=".presentation.activity.ProductDetailActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.activity.SearchProductActivity"
            android:exported="false"/>

        <activity android:name=".presentation.activity.ChangePasswordActivity"
            />
        
        <activity android:name=".presentation.activity.EditProfileActivity"
            android:exported="false"/>
        
        <activity android:name=".presentation.activity.AddUserAddressActivity"
            android:exported="false"/>

        <activity android:name=".presentation.activity.UserAddressesActivity"
            android:exported="false"/>

        <activity
            android:name=".presentation.activity.OrderCreateActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.activity.OrderSuccessActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".presentation.activity.VNPayPaymentActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity
            android:name=".presentation.activity.PaymentProcessingActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".presentation.activity.PaymentSuccessActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".presentation.activity.PaymentFailedActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".presentation.activity.OrderViewListActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".presentation.activity.OrderViewDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".presentation.activity.AddStoreActivity"
            android:exported="false"
            android:label="Thêm cửa hàng mới" />

        <!-- Chat Activity -->
        <activity
            android:name=".presentation.activity.ChatActivity"
            android:exported="false"
            android:label="Chat" />

        <!-- KHÔNG khai báo fragment ở đây -->
    </application>
</manifest>
