<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Màu nền (trong suốt trong trường hợp này) -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" /> <!-- Màu và độ rộng của viền -->
    <corners android:radius="0dp" /> <!-- Tùy chọn: bo tròn góc -->
</shape>
